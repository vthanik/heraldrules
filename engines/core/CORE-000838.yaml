Authorities:
  - Organization: CDISC
    Standards:
      - Name: USDM
        References:
          - Citations:
              - Cited Guidance: 'An encounter must only reference encounters that are specified
                  within the same study design.'
                Document: 'USDM_CORE_Rules.xlsx'
            Origin: 'USDM Conformance Rules'
            Rule Identifier:
              Id: 'DDF00029'
              Version: '1'
            Version: '1.0'
        Version: '3.0'
      - Name: USDM
        References:
          - Citations:
              - Cited Guidance: 'An encounter must only reference encounters that are specified
                  within the same study design.'
                Document: 'USDM_CORE_Rules.xlsx'
            Origin: 'USDM Conformance Rules'
            Rule Identifier:
              Id: 'DDF00029'
              Version: '1'
            Version: '1.0'
        Version: '4.0'
Check:
  all:
    - name: instanceType
      operator: equal_to
      value: 'Encounter'
    - name: rel_type
      operator: equal_to
      value: 'definition'
    - any:
        - all:
            - name: previousId
              operator: exists
            - name: previousId
              operator: non_empty
            - name: previousId
              operator: is_not_contained_by
              value: $encounter_ids_for_study_design
        - all:
            - name: nextId
              operator: exists
            - name: nextId
              operator: non_empty
            - name: nextId
              operator: is_not_contained_by
              value: $encounter_ids_for_study_design
Core:
  Id: 'CORE-000838'
  Status: Published
  Version: '1'
Description: 'An encounter must only reference encounters that are specified
  within the same study design.'
Executability: Fully Executable
Operations:
  - group:
      - parent_id
      - rel_type
    id: $encounter_ids_for_study_design
    name: id
    operator: distinct
Outcome:
  Message: 'The encounter references a previous or next encounter that is not
    specified within the same study design.'
  Output Variables:
    - parent_entity
    - parent_id
    - parent_rel
    - id
    - name
    - previousId
    - nextId
    - $encounter_ids_for_study_design
Rule Type: Record Data
Scope:
  Entities:
    Include:
      - 'Encounter'
Sensitivity: Record
