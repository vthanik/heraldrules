# Variable: --LLT, --LLTCD
# Condition:
# Rule: --LLTCD and --LLT have a one-to-one relationship 
Authorities:
  - Organization: FDA
    Standards:
      - Name: SDTMIG
        References:
          - Citations:
              - Cited Guidance: All paired variables should have a one-to-one relationship.
                  Examples include short name and name of test; parameter name
                  and parameter code or number; variable name and variable
                  label, etc.
                Document: FDA
                Section: FDAB009
            Origin: FDA Business Rules
            Rule Identifier:
              Id: FB0906
              Version: '1'
            Version: '1.5'
        Version: '3.2'
      - Name: SDTMIG
        References:
          - Citations:
              - Cited Guidance: All paired variables should have a one-to-one relationship.
                  Examples include short name and name of test; parameter name
                  and parameter code or number; variable name and variable
                  label, etc.
                Document: FDA
                Section: FDAB009
            Origin: FDA Business Rules
            Rule Identifier:
              Id: FB0906
              Version: '1'
            Version: '1.5'
        Version: '3.3'
      - Name: SDTMIG
        References:
          - Citations:
              - Cited Guidance: All paired variables should have a one-to-one relationship.
                  Examples include short name and name of test; parameter name
                  and parameter code or number; variable name and variable
                  label, etc.
                Document: FDA
                Section: FDAB009
            Origin: FDA Business Rules
            Rule Identifier:
              Id: FB0906
              Version: '1'
            Version: '1.5'
        Version: '3.4'
Check:
  all:
    - name: --LLT
      operator: exists
    - name: --LLTCD
      operator: exists
    - name: --LLT
      operator: is_not_unique_relationship
      value: --LLTCD
Core:
  Id: CORE-000706
  Status: Published
  Version: '1'
Description: --LLTCD and --LLT should have a one-to-one relationship.
Executability: Fully Executable
Outcome:
  Message: --LLTCD and --LLT do not have a one-to-one relationship.
  Output Variables:
    - --LLT
    - --LLTCD
Rule Type: Record Data
Scope:
  Classes:
    Include:
      - EVENTS
  Domains:
    Include:
      - ALL
Sensitivity: Record
